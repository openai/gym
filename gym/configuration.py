def logger_setup():
    print('undo_logger_setup is DEPRECATED. If you want to configure logging, use e.g. "from gym import logger; logger.set_level(logger.INFO)"')

def undo_logger_setup():
    print('undo_logger_setup is DEPRECATED. If you want to turn off logging, use "from gym import logger; logger.set_level(logger.DISABLED)"')
